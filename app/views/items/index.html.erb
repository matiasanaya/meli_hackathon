<h1>Listing items</h1>

<table>
  <tr>
    <th>Mercado</th>
    <th>User</th>
    <th>Has stock</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.mercado_id %></td>
    <td><%= item.user_id %></td>
    <td><%= item.has_stock %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Edit', edit_item_path(item) %></td>
    <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_item_path %>
